import React from 'react';
import ReactDOM from 'react-dom';

const Application = () => {
    return (
        <div>hello maverick!</div>
    );
};

ReactDOM.render(<Application />, document.querySelector('#root'));
